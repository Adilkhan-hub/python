# 1 min = 60
# 1 hour = 60 * 60 = 3600

count = input("Введите количество секунд для преобразования: ")
t = int(count)
h = t // 3600
m = (t - (h * 3600)) // 60
s = (t - ((h * 3600) + (m * 60)))

print(f"{h:.0f}:{m:.0f}:{s:.0f}")
